// string templates
// string literals

const subject = 'tree';

// regular 1️⃣
const x = 'x: The ' + subject + ' is big';

// optional 2️⃣
const y = `y: The ${subject} is big`;

console.log(x);
console.log(y);